{
  "name": "definable",
  "displayName": "DefCode",
  "description": "AI-powered coding assistant — open DefCode CLI in the sidebar",
  "version": "0.0.5",
  "publisher": "definable-ai",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "ai",
    "coding",
    "assistant",
    "defcode",
    "cli"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "icon": "icon.png",
  "contributes": {
    "commands": [
      {
        "command": "definable.open",
        "title": "DefCode: Open",
        "icon": "$(sparkle)"
      },
      {
        "command": "definable.openInNewTab",
        "title": "DefCode: New Session",
        "icon": "$(add)"
      },
      {
        "command": "definable.closeAll",
        "title": "DefCode: Close All Sessions"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "definable-sidebar",
          "title": "DefCode",
          "icon": "icon.svg"
        }
      ]
    },
    "views": {
      "definable-sidebar": [
        {
          "type": "webview",
          "id": "definable.sessionsView",
          "name": "DefCode",
          "visibility": "visible"
        }
      ]
    },
    "keybindings": [
      {
        "command": "definable.open",
        "key": "ctrl+shift+d",
        "mac": "cmd+shift+d"
      },
      {
        "command": "definable.openInNewTab",
        "key": "ctrl+shift+n",
        "mac": "cmd+shift+n",
        "when": "definable.active"
      }
    ],
    "configuration": {
      "title": "DefCode",
      "properties": {
        "definable.binaryPath": {
          "type": "string",
          "default": "",
          "description": "Custom path to the def binary. Leave empty to use PATH."
        }
      }
    }
  },
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "package": "npm run build && vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "vsce": "^2.15.0"
  }
}